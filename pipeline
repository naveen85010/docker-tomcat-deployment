pipeline {
    agent any
    
    stages{
       
     stage('Git check out'){
        steps{
        git 'https://github.com/naveen85010/docker-tomcat-deployment.git'
     }
     }
    
       
      stage('Building docker image'){
        steps{
            sh 'docker build -t $JOB_NAME:v1.$BUILD_NUMBER .'
            sh 'docker tag $JOB_NAME:v1.$BUILD_NUMBER naveen850/$JOB_NAME:v1.$BUILD_NUMBER'
            sh 'docker tag $JOB_NAME:v1.$BUILD_NUMBER naveen850/$JOB_NAME:latest'
            

        }
      }

      stage('Pushing docker image to docker hub'){
        steps{
            withCredentials([string(credentialsId: 'Dockerpass', variable: 'Dockerpass')]) {
    // some block
          sh "docker login -u marella04 -p ${Dockerpass}"
          sh 'docker push naveen850/$JOB_NAME:v1.$BUILD_NUMBER'
          sh 'docker push naveen850/$JOB_NAME:latest'
      }
        }
      }

    }
}